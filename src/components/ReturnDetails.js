import React from "react";

class ReturnDetails extends React.Component {
    render() {
        return (
            <div>
                <h4>Return Details</h4>
                <p>This item can be returned to any Target store or Target.com.</p>
                <p>This item must be returned within 90 days of the date it was purchased in store, shipped, delivered by a Shipt shopper, or made ready for pickup. View return policy for details.
                See the <a href="https://help.target.com/help/subcategoryarticle?childcat=Returns&parentcat=Returns+%26+Exchanges&searchQuery=search+help">return policy</a> for complete information.</p>
            </div>
        )
    }
}

export default ReturnDetails